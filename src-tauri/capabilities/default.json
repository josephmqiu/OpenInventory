{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default capability for the main inventory monitor window.",
  "windows": ["main"],
  "permissions": [
    "core:window:default",
    "core:event:default",
    "notification:default"
  ]
}
